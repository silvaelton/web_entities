= simple_form_for @entity do |f|
  .row
    .col-md-12
      = f.input :name
  .row
    .col-md-12
      = f.submit :button, class: 'btn btn-success', data: { disable_with: t(:disable_with)}
      = link_to "Voltar para listagem", admin.cadastres_path, data: { disable_with: t(:disable_with)}